pipeline {
    agent {
        label 'jenkins-agent'
    }
    tools {
        jdk 'jdk17'
        maven 'Maven3'
    }
    stages {
        stage('Checkout from scm'){
            steps{
                git branch: 'main', url: 'https://github.com/gogulanikhil/devops-full-project'
            }
        }
    }
}