pipeline {
    agent {
        label 'jenkins-agent'
    }
    tools {
        jdk 'jdk17'
        maven 'Maven3'
    }
    stages {
        stage('Checkout from scm'){
            steps{
                git branch: 'main', credentialsId: 'github' url: 'https://github.com/gogulanikhil/devops-full-project'
            }
        }
    }
}